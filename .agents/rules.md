# Antigravity Agent Rules

- Luon doc PRD (`docs/`) truoc khi de xuat thay doi kien truc.
- Khong sua truc tiep `src/models/schemas.py` neu chua cap nhat cac node agent lien quan trong `src/agents/nodes/`.
- Moi thay doi lien quan Verification Agent phai kem theo test trong `tests/test_agents/`.
- Ghi log moi phien lam viec vao `.ai-log/antigravity.jsonl` qua `scripts/log_antigravity.py`.
