# Workflow: Review pipeline agent

1. Doc `src/agents/graph.py` de nam luong xu ly hien tai.
2. Chay `pytest tests/test_agents -v`.
3. Neu them agent moi, cap nhat `docs/architecture_diagram.md`.
4. Chay `python scripts/log_antigravity.py` truoc khi commit.
